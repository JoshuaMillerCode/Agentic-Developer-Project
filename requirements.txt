# Agentic Developer Project — Python dependencies
# (MCP server: mcp-server/requirements.txt | LLM backend: backend/requirements.txt)
# From project root: pip install -r mcp-server/requirements.txt && pip install -r backend/requirements.txt
